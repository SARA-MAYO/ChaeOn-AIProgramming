# 🚀 실행 가이드 (채점·재현용)

이 문서 하나만 따라 하면 기능 1·2·3을 처음부터 끝까지 실행할 수 있습니다.
환경은 **Google Colab** 기준이며, **반드시 기능 1 → 기능 2 → 기능 3 순서**로 실행합니다.

> 왜 순서가 중요한가요? 기능 3은 기능 1·2가 학습해 **구글 드라이브에 저장한 모델**을 불러와 실제 채팅을 분석합니다.
> 기능 1·2를 먼저 돌리지 않으면 기능 3은 "모델이 없다"며 멈춥니다.

---

## 0단계 — 별도 전달 파일 배치 (⚠️ 깃에 없음)

아래 원본 데이터는 라이선스·개인정보 문제로 **GitHub에 올리지 않았고, 제출물과 함께 별도로 전달**합니다.
각 노트북을 열고 **Colab 세션(`/content/`)에 업로드**하세요. (파일명이 정확히 같아야 노트북이 읽습니다.)

| 전달 파일명 | 사용 기능 | 둘 위치 | 실행 필수 |
|---|---|---|---|
| `감성대화말뭉치(최종데이터)_Training.xlsx` | 기능 1 | Colab `/content/` | ✅ 필수 |
| `감성대화말뭉치(최종데이터)_Validation.xlsx` | 기능 1 | Colab `/content/` | ✅ 필수 |
| `talksets-train-1_aihub.csv` | 기능 2 | Colab `/content/` | ✅ 필수 |
| (카카오톡 원본 채팅 파일) ★ | 기능 3 (전처리 원본) | — | ⛔ 실행엔 불필요 |

> ★ 기능 3의 실제 입력(`feature3/chat_log1_raw.json`)은 **이미 익명화·전처리되어 저장소에 포함**되어 있습니다.
> 따라서 기능 3 실행에는 카카오톡 원본이 **필요 없습니다.** 원본은 "전처리 증빙 / 재실행용"으로만 전달하며,
> 다시 전처리하려면 `feature3/preprocess_chat_log.py`로 `카카오톡원본.csv → chat_log1_raw.json` 변환을 재현할 수 있습니다.

**업로드 방법(Colab):** 왼쪽 📁 아이콘 → 파일 영역에 드래그&드롭, 또는 우클릭 → 업로드.
(세션이 끊기면 업로드 파일은 사라지므로, 그 기능을 다시 돌릴 땐 재업로드합니다.)

---

## 실행 순서 (Run all 3번)

모델은 모두 **본인 구글 드라이브(MyDrive)** 를 통해 자동 연동됩니다.
사람이 하는 일은 **① 데이터 업로드 ② 드라이브 인증 클릭 ③ Run all** 뿐입니다.

### 1) 기능 1 — 감정 분석  `feature1/sentiment_analysis.ipynb`
1. Colab에서 열기 → 런타임 유형 **GPU(T4)** 설정
2. `감성대화말뭉치..._Training.xlsx`, `..._Validation.xlsx`를 `/content/`에 업로드
3. **런타임 → 모두 실행(Run all)**
4. 학습 끝나면 모델이 드라이브에 **자동 저장**:
   `MyDrive/chaeon_feature1_checkpoint/`, `svm_model.pkl`, `vectorizer.pkl`

### 2) 기능 2 — 공격성 탐지  `feature2/aggression_detection.ipynb`
1. Colab에서 열기 → **GPU(T4)** 설정
2. `talksets-train-1_aihub.csv`를 `/content/`에 업로드
3. **Run all**
4. 모델이 드라이브에 자동 저장: `MyDrive/chaeon_feature2_model/`

### 3) 기능 3 — 상태 변화 분석  `feature3/colab_feature3_member1.ipynb`
1. Colab에서 열기 → **Run all** (업로드할 데이터 없음)
2. 자동으로: 저장소 clone → 드라이브 인증 클릭 → 기능 1·2 모델 자동 로드
   → 실제 채팅(`chat_log1_raw.json`) → 기능 1·2 라벨링 → 날짜별 분석
3. 결과: **`daily_report.json` 생성** + 화면에 표/멘트 시각화

> 기능 3은 시작 시 기능 1·2 모델 4개 파일이 드라이브에 있는지 **자동 검사**하고,
> 없으면 어떤 파일이 부족한지 안내하고 멈춥니다. (→ 그 기능 노트북을 먼저 Run all)

---

## 실행하면 생성되는 파일 (기능별)

> 모델/체크포인트 등 용량 큰 산출물은 **코드 실행 시 재생성**되므로 제출 필수는 아닙니다.
> 재현에 꼭 필요한 것은 **코드 + 데이터 + 시드(42)** 입니다.

### 기능 1
| 파일/폴더 | 위치 | 내용 |
|---|---|---|
| `log.txt` | `/content/` (feature1) | 실행 시작~최종 metric 전체 로그 |
| `confusion_matrix_sentiment_only.png` | `/content/` | 혼동행렬 이미지 |
| `final_model/`, `saved_models/` | `/content/` | 학습 모델·체크포인트 (임시) |
| `chaeon_feature1_checkpoint/`, `svm_model.pkl`, `vectorizer.pkl` | **MyDrive** | 기능 3이 불러올 모델 (영구) |

### 기능 2
| 파일/폴더 | 위치 | 내용 |
|---|---|---|
| `log.txt` | `/content/` (feature2) | 실행 로그 |
| `aggression_processed_subset.csv` | `/content/` | 전처리 완료 데이터셋 (재생성됨) |
| `results/` | `/content/` | 학습 체크포인트 (임시) |
| `chaeon_feature2_model/` | **MyDrive** | 기능 3이 불러올 모델 (영구) |

### 기능 3
| 파일 | 위치 | 내용 |
|---|---|---|
| `sample_chat_log.json` | `/content/.../feature3/` | 기능 1·2가 라벨링한 결과 (감정·공격성 부착) |
| `daily_report.json` | `/content/.../feature3/` **+ MyDrive** | **날짜별 × 사용자별 상태 리포트 (최종 산출물)** |

> 기능 3은 학습이 없어 `result.txt`는 만들지 않습니다. (기능 1·2에는 `result.txt`가 있습니다 — 시드/최종 metric 기록)

---

## (옵션) GPU·모델 없이 기능 3만 빠르게 보기

기능 1·2 모델 없이 **기능 3 로직만** 확인하려면, 라벨이 이미 붙은 샘플로 단독 실행할 수 있습니다.
```bash
cd feature3
python state_change_analysis.py     # 입력: sample_chat_log.json → 리포트 생성
```
파이썬 표준 라이브러리만 사용하므로 별도 설치·GPU가 필요 없습니다.

---

## 자주 묻는 문제

- **기능 3이 "모델이 없다"며 멈춰요** → 기능 1·2 노트북을 먼저 **Run all** 해서 드라이브에 모델을 만든 뒤 다시 실행하세요.
- **드라이브 인증 창이 떠요** → 본인 구글 계정으로 **허용 클릭**하면 됩니다. (모델 저장·로드에 필요)
- **업로드한 데이터가 사라졌어요** → Colab 세션이 끊기면 `/content/` 업로드 파일은 삭제됩니다. 그 기능을 다시 돌릴 때 재업로드하세요.
- **`daily_report.json`이 안 보여요** → 생성 후 왼쪽 📁 파일 영역 **새로고침**, 또는 `MyDrive` 최상단에서 확인하세요.

---

## 재현성 · 적법성 요약

| 항목 | 값 |
|---|---|
| Seed | 42 (random / numpy / torch / transformers) |
| 분할 | Train / Validation / Test = 80 / 10 / 10 (Stratified) |
| 평가지표 | Accuracy, Macro-F1, Confusion Matrix |

- **학습**: 기능 1·2 모두 **Train + Validation 만** 사용 (Test 누출 없음)
- **테스트**: 학습 종료 후 **Test 로 1회만** 측정
- 상세: `feature1/result.txt`, `feature2/result.txt`, 각 `featureN/README.md` 참고
</content>
</invoke>
