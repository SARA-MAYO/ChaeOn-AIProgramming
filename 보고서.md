# 채온(CHAEON) — 최종 보고서

> 명지대학교 AI프로그래밍 팀 프로젝트 / 팀명: 채온(CHAEON)
> ⚠️ 초안입니다. 핵심 내용 위주로 정리했으며, 표현·세부 수치는 발표 전 다듬을 예정입니다.
> ⚠️ 일부 수치(기능1 후보 비교표의 `__.____` 칸)는 `feature1/model_selection_experiment.ipynb` 를 Colab에서 Run all 한 뒤 `test_result.txt` 값으로 채울 예정입니다.
> 📊 그래프 삽입 위치는 본문에 `[그래프 G#]` 로 표시했고, 생성용 프롬프트는 문서 맨 끝 **[부록 A]** 에 정리했습니다.

---

## 1. 프로젝트 개요

### 주제
팀 프로젝트 단체 채팅 로그를 분석하여, 구성원의 **커뮤니케이션 상태 악화 가능성을 조기에 탐지**하는 AI 시스템.

### 해결하고자 한 문제
팀 협업에서 갈등·번아웃·참여 저하는 **겉으로 드러나기 전에 채팅 톤·참여량 변화로 먼저 나타난다.**
하지만 사람이 모든 대화를 추적해 "평소와 달라진 시점"을 잡아내기는 어렵다.
채온은 채팅 메시지의 **감정·공격성**을 자동 분류하고, 이를 **시간 축으로 누적·비교**하여
"평소와 얼마나 다른가"를 신호등(🟢🟡🟠⚪)으로 알려줌으로써 조기 신호를 제공한다.

### 입력과 출력 (전체 파이프라인)

```
[원문 채팅 메시지]
   │  (sender_id, timestamp, text)
   ▼
┌─────────────────────────────┐
│ 기능1 — 감정 분석            │  text → positive(1) / negative(0)
│ 기능2 — 공격성 탐지          │  text → 0(비공격) / 1(약) / 2(강)
└─────────────────────────────┘
   │  (라벨이 붙은 메시지 리스트)
   ▼
┌─────────────────────────────┐
│ 기능3 — 상태 변화 분석       │  직전 7일 평소 대비 변화 계산
└─────────────────────────────┘
   ▼
[날짜별 × 발신자별 상태 리포트]
   🟢 평소와 비슷 / 🟡 조금 다름 / 🟠 꽤 다름 / ⚪ 데이터 부족
```

> 📊 **[그래프 G1 — 시스템 파이프라인 다이어그램]** 위 ASCII 흐름을 깔끔한 도식으로 교체 (부록 A-G1)

| 단계 | 입력 | 출력 |
|---|---|---|
| 기능1 | 채팅 메시지(문장) | 긍정(1) / 부정(0) |
| 기능2 | 채팅 메시지(문장) | 비공격(0) / 약한공격(1) / 강한공격(2) |
| 기능3 | 라벨이 붙은 메시지 리스트(발신자·시각·감정·공격성) | 날짜별·발신자별 상태 판정 + 자연어 해석 |

---

## 2. 데이터셋 설명

### 기능1 — 감정 분석
- **출처**: AIHub 감성대화 말뭉치 (https://www.aihub.or.kr/)
- **특징·규모**:
  - 원본 통합 58,271행 (`감정_대분류` + `사람문장1~3`)
  - 라벨링: `기쁨` → 긍정(1) / 그 외 감정 → 부정(0) — **이진 분류(2 클래스)**
  - 원본 분포가 부정에 크게 치우침 (긍정 7,339 / 부정 50,932)
- **전처리**:
  1. 두 xlsx에서 필요한 컬럼만 읽어 통합, 사람문장 1·2·3을 한 문장으로 결합
  2. `text` 기준 중복 제거 (train/test 누출 방지)
  3. 이진 라벨링
  4. **1:1 다운샘플링**(seed=42) → 14,678행 (긍정 7,339 / 부정 7,339)로 클래스 균형
  5. **Stratified 8:1:1 분할**(seed=42) → Train 11,742 / Val 1,468 / Test 1,468

> 📊 **[그래프 G2 — 기능1 클래스 분포(다운샘플링 전/후)]** (부록 A-G2)

### 기능2 — 공격성 탐지
- **출처**: AIHub 텍스트 윤리 검증 데이터셋 (https://www.aihub.or.kr/)
- **특징·규모**:
  - 원본 70,593행 (`text`, `intensity` 등)
  - 라벨링: intensity 점수 → **3단계 binning(3 클래스)**
    - intensity < 1.0 → 비공격(0)
    - 1.0 ≤ intensity < 1.8 → 약한공격(1)
    - intensity ≥ 1.8 → 강한공격(2)
  - 정제 후 69,309행 (0=29,487 / 1=21,995 / 2=17,827)
- **전처리**:
  1. 로드 후 intensity 기준 3단계 라벨링
  2. 결측·공백·중복(text 기준) 제거 → 69,309행
  3. **다운샘플링 생략** — 실제 분포를 살리기 위해 원본 규모 유지 (기능1과 반대 전략)
  4. **Stratified 8:1:1 분할**(seed=42) → Train 55,447 / Val 6,931 / Test 6,931

> 📊 **[그래프 G3 — 기능2 3클래스 분포]** (부록 A-G3)

### 기능3 — 상태 변화 분석 (실제 채팅 로그)
- **출처**: 팀원의 지인이 진행한 **실제 카카오톡 단체 채팅** (익명화 전제로 사용 동의 확인)
- **특징**: 학습용 데이터셋이 아니라 **시스템 입력**으로 사용 (정답 라벨 없음)
- **전처리·익명화** (`preprocess_chat_log.py`):
  1. timestamp 한국식 표기 → ISO 8601(KST) 변환
  2. 이모티콘·사진·동영상 등 미디어/시스템 placeholder 제거
  3. 발신자 `User_A`~`User_D` 가명화 + 전화번호·이메일·계좌 등 **PII 마스킹**
  - 결과물: `chat_log1_raw.json`, `chat_log2_raw.json` (익명화 완료본만 저장소 포함)

> 공통 설정: **Seed=42**, **Stratified 80:10:10**, 평가지표 **Accuracy / Macro-F1 / Confusion Matrix**

---

## 3. 베이스라인 모델

### 기능1 — 후보 모델 비교 (model_selection_experiment.ipynb)
감정 분석은 단일 모델을 바로 정하지 않고, **세 후보를 동일한 Test set으로 비교**하여 최종 모델을 선정했다.
(세 후보 모두 Seed=42, Stratified 8:1:1, 동일 Train/Val/Test 사용)

| 후보 | 구성 | 설정 |
|---|---|---|
| 후보1 | KLUE-RoBERTa 단독 | `klue/roberta-base`, 이진 분류 파인튜닝, max_length=128, lr=2e-5, epochs=3 |
| 후보2 | KcELECTRA + SVM 7:3 앙상블 | KcELECTRA(0.7) + SVM·TF-IDF(0.3) Soft Voting |
| 후보3 | KcELECTRA + Bi-LSTM 7:3 앙상블 | KcELECTRA(0.7) + Bi-LSTM(0.3) Soft Voting |

| 후보 | Accuracy | Macro-F1 |
|---|---|---|
| 후보1) KLUE-RoBERTa 단독 | `__.____` | `__.____` |
| 후보2) KcELECTRA + SVM 7:3 | `__.____` | `__.____` |
| 후보3) KcELECTRA + Bi-LSTM 7:3 | `__.____` | `__.____` |

> ⚠️ 위 수치는 `feature1/model_selection_experiment.ipynb` 를 Colab에서 Run all 한 뒤,
> 생성되는 `test_result.txt` 값으로 채울 것. (세 후보 성능은 근소했으며, 가장 높은 **KcELECTRA + SVM 7:3** 을 최종 채택)

> 📊 **[그래프 G4 — 기능1 세 후보 성능 비교]** (숫자 채운 뒤 생성 / 부록 A-G4)

### 기능2 — TF-IDF + 로지스틱 회귀
공격성 탐지의 비교 기준선으로 가벼운 전통 머신러닝 모델을 사용했다.

| 항목 | 설정 |
|---|---|
| 벡터화 | `TfidfVectorizer(max_features=5000)` |
| 분류기 | `LogisticRegression(max_iter=1000, random_state=42)` |
| 데이터 분할 | KoELECTRA와 **동일한** Stratified 8:1:1 (seed=42) 재사용 |
| 평가 | Accuracy, Macro-F1 (메인 모델과 동일 기준) |

- **베이스라인 성능 (Test 6,931개)**: **Accuracy 0.5501**

---

## 4. 개발 모델

### 기능1 — KcELECTRA + SVM 7:3 Soft Voting 앙상블

| 구분 | 모델 | 설정 |
|---|---|---|
| 메인 | **KcELECTRA** | `beomi/KcELECTRA-base-v2022`, 이진 분류 파인튜닝, 가중치 0.7 |
| 보조 | **SVM** | `SVC(probability=True, random_state=42)` + `TfidfVectorizer(max_features=5000)`, 가중치 0.3 |
| 결합 | **Soft Voting** | 두 모델의 확률을 7:3 가중 합산하여 최종 예측 |
| 학습 설정 | KcELECTRA | lr=2e-5, batch=16, epochs=3, weight_decay=0.01, max_length=128 |

- **베이스라인 대비 개선**: KLUE-RoBERTa 단독·KcELECTRA+Bi-LSTM 등 후보와 비교한 결과, **딥러닝(KcELECTRA) + 전통 ML(SVM)을 7:3으로 앙상블**한 구성이 가장 높은 Macro-F1을 보여 최종 채택.
- **적용 아이디어**: 문맥을 이해하는 딥러닝(KcELECTRA)과 단어 빈도 기반 전통 ML(SVM)을 **앙상블**하여 서로의 약점을 보완.

| 모델 | Accuracy | Macro-F1 | 비고 |
|---|---|---|---|
| 후보1) KLUE-RoBERTa 단독 | `__.____` | `__.____` | 비교 실험 |
| 후보3) KcELECTRA + Bi-LSTM 7:3 | `__.____` | `__.____` | 비교 실험 |
| **개발 모델) KcELECTRA + SVM 7:3** | **0.9714** | **0.9714** | 최종 채택 (정식: result.txt) |

- **최종 성능 (Test 1,468개)**: **Accuracy 0.9714 / Macro-F1 0.9714**
  - 부정(0): P 0.9651 / R 0.9782 / F1 0.9716
  - 긍정(1): P 0.9779 / R 0.9646 / F1 0.9712

> 🖼️ **[Confusion Matrix — 기능1]** 노트북 실행 시 자동 생성되는 `confusion_matrix_sentiment_only.png` 를 그대로 삽입 (Claude 웹 불필요)

> ⚠️ 후보1·후보3 칸은 `model_selection_experiment.ipynb` 실행 후 `test_result.txt` 값으로 채울 것.
> 개발 모델 수치(0.9714)는 정식 학습 노트북(`sentiment_analysis.ipynb` / `result.txt`) 기준이며, 비교 실험의 후보2 수치와 소수점 단위에서 다를 수 있음.

### 기능2 — KoELECTRA-small (3-class)

| 구분 | 모델 | 설정 |
|---|---|---|
| 메인 | **KoELECTRA-small** | `monologg/koelectra-small-v3-discriminator`, `num_labels=3`, max_length=128 |
| 모델선택 | Validation Macro-F1 기준 best epoch (`load_best_model_at_end=True`, `metric_for_best_model="f1"`) |
| 학습 설정 | lr=3e-5, batch=32, epochs=3, weight_decay=0.01, max_length=128 |

- **베이스라인 대비 개선**: TF-IDF+로지스틱회귀가 단어 표면 정보만 보는 한계를, **한국어 사전학습 딥러닝 모델**로 문맥까지 반영해 개선.

| 모델 | Accuracy | 비고 |
|---|---|---|
| 베이스라인 (TF-IDF + 로지스틱회귀) | 0.5501 | 동일 8:1:1 분할 |
| **개발 모델 (KoELECTRA-small)** | **0.64** | **+0.09 향상** |

> 📊 **[그래프 G5 — 기능2 베이스라인 vs 개발모델 Accuracy]** (부록 A-G5)

- **개발 모델 최종 성능 (Test 6,931개)**: Accuracy 0.64 / Macro-F1 0.60
  - 비공격(0): P 0.74 / R 0.88 / F1 0.80
  - 약한공격(1): P 0.51 / R 0.51 / F1 0.51
  - 강한공격(2): P 0.61 / R 0.41 / F1 0.49

> 📊 **[그래프 G6 — 기능2 클래스별 F1]** (약/강 공격이 약점임을 시각화 / 부록 A-G6)
> 🖼️ **[Confusion Matrix — 기능2]** 노트북 자동 생성 PNG(3×3)를 그대로 삽입 (Claude 웹 불필요)

- **그 외 적용 아이디어**: 기능3 연동을 위해 3-class와 별도로 **공격 vs 비공격 Binary 지표**도 함께 산출
  - Binary: Accuracy 0.8145 / Precision 0.8942 / Recall 0.7680 / F1 0.8263

### 기능3 — 규칙 기반 상태 변화 분석 (학습 없음)
기능1·2의 출력을 받아 **시간 축 변화**를 계산하는 규칙 기반 엔진. (별도 학습/테스트셋 미사용)

- **3대 지표**: ① 부정 감정 변화(%p) ② 공격성 변화(%p) ③ 참여량 변화(%)
- **방식**: 분석 당일 vs **직전 7일 baseline** 비교 → 임계값으로 상태 판정
- **가드레일**: 메시지 수가 기준 미만이면 ⚪(데이터 부족)으로 처리해 과잉 해석 방지
- **출력**: 날짜별 × 발신자별 🟢🟡🟠⚪ 판정 + 자연어 해석

> 📊 **[그래프 G7 — 기능3 상태 리포트 히트맵(예시)]** 날짜×발신자 신호등 시각화 (부록 A-G7)

### 팀 차원의 어필 포인트
- **엔드투엔드 파이프라인**: 원문 → 감정/공격성 분류 → 시계열 상태 분석까지 하나로 연결
- **모델 다양성**: 딥러닝(ELECTRA 2종) + 전통 ML(SVM·로지스틱) + 앙상블 + 규칙기반 통계
- **재현성·적법성 일관 적용**: 전 기능 Seed=42, Stratified 8:1:1, 테스트셋 1회만 측정(누출 방지)
- **실데이터 적용**: 실제 카카오톡 로그를 익명화·PII 마스킹하여 현실 시나리오 검증

---

## 5. 논의

### 한계점
- **공격성 약/강 구분 정확도 한계**: 기능2의 약한공격(F1 0.51)·강한공격(F1 0.49)이 낮음. intensity 경계(1.0/1.8)가 모호하고 두 클래스의 표현이 겹쳐 혼동이 큼.
- **감정 분류의 단순화·데이터 손실**: 기능1은 다중 감정을 긍정/부정 2진으로 축소했고, 균형을 위해 부정 50,932 → 7,339로 **다운샘플링하여 상당량을 버림**. 높은 정확도(0.9714)는 이 단순화·균형화의 영향도 고려해야 함.
- **기능3 정량 평가 부재**: 규칙 기반이라 정답 라벨이 없는 실데이터에 대해 성능을 수치로 검증하기 어려움. 임계값이 통계적 검정이 아닌 휴리스틱.
- **도메인 갭**: 학습 데이터(감성대화·윤리검증 말뭉치)와 실제 팀 채팅의 말투·맥락 차이로 실데이터 성능 저하 가능.
- **기능1 후보 성능 근소**: 세 후보(KLUE-RoBERTa·KcELECTRA+SVM·KcELECTRA+Bi-LSTM)의 성능 차이가 크지 않아, 모델 구성에 따른 이득이 제한적이었음. (최고치 기준으로 KcELECTRA+SVM 채택)

### 향후 개선 방향
- 트리 계열(RandomForest/LightGBM) 등 후보를 추가해 비교 폭 확대
- intensity 경계 재조정 또는 클래스 가중치 적용으로 약/강 공격 구분 개선
- 기능3 임계값을 통계 기반(분포·표준편차)으로 정교화

### 팀원별 역할 분담
> *(실제 이름·담당으로 채워 넣을 것 — 초안 템플릿)*

| 팀원 | 담당 | 주요 작업 |
|---|---|---|
| 팀원 A | 기능1 감정 분석 | 데이터 전처리, KcELECTRA 파인튜닝, SVM 앙상블 구현·평가 |
| 팀원 B | 기능2 공격성 탐지 | intensity 라벨링, KoELECTRA 학습, 베이스라인 비교·평가 |
| 팀원 C | 기능3 상태 변화 분석 | 지표 설계, 규칙 엔진 구현, 통합 노트북·실데이터 전처리 |
| 공통 | 통합·문서화 | 파이프라인 연동(Drive), README·보고서·발표자료 |

---

## 부록 A. 그래프 생성 프롬프트 (Claude 웹용)

> 아래 프롬프트를 Claude 웹에 그대로 붙여넣어 이미지를 생성한 뒤, 본문의 해당 `[그래프 G#]` 위치에 삽입하세요.
> Confusion Matrix(기능1·2)는 노트북 실행 시 PNG로 자동 생성되므로 별도 생성이 필요 없습니다.

**A-G1. 시스템 파이프라인 다이어그램**
```
채팅 기반 팀 커뮤니케이션 분석 시스템의 파이프라인을 가로 흐름 도식으로 그려줘.
왼쪽부터: [원문 채팅 메시지 (발신자·시각·텍스트)] →
두 개 병렬 박스 [기능1 감정분석 → 긍정/부정], [기능2 공격성탐지 → 비공격/약/강] →
[기능3 상태변화 분석 → 직전 7일 대비 변화] →
[날짜별·발신자별 상태 리포트 → 🟢평소와비슷 🟡조금다름 🟠꽤다름 ⚪데이터부족].
화살표로 연결, 깔끔한 플랫 디자인, 한글 라벨, 16:9 비율.
```

**A-G2. 기능1 클래스 분포 (다운샘플링 전/후)**
```
막대그래프 2개를 나란히 그려줘. 전체 제목 '기능1 감정 데이터 클래스 분포'.
- 왼쪽 (다운샘플링 전): 긍정 7,339 / 부정 50,932
- 오른쪽 (1:1 다운샘플링 후): 긍정 7,339 / 부정 7,339
y축은 메시지 수, 막대 위에 수치 표기, 긍정/부정 색 구분, 한글 라벨.
'부정 50,932 → 7,339로 균형화'를 한눈에 보이게.
```

**A-G3. 기능2 3클래스 분포**
```
세로 막대그래프 1개. 제목 '기능2 공격성 3단계 클래스 분포 (정제 후 69,309개)'.
- 비공격(0): 29,487
- 약한공격(1): 21,995
- 강한공격(2): 17,827
막대 위 수치 표기, 단계가 올라갈수록 진한 색, 한글 라벨.
```

**A-G4. 기능1 세 후보 성능 비교** *(model_selection_experiment.ipynb 실행 후 숫자 채워서 요청)*
```
그룹 막대그래프. 제목 '기능1 모델 후보 비교 (동일 Test set, Seed=42)'.
3개 후보 각각 Accuracy와 Macro-F1 두 막대:
- KLUE-RoBERTa 단독: Accuracy __, Macro-F1 __
- KcELECTRA + SVM 7:3: Accuracy __, Macro-F1 __   ← 이 후보를 강조색으로
- KcELECTRA + Bi-LSTM 7:3: Accuracy __, Macro-F1 __
y축 0~1, 막대 위 수치, 한글 라벨. (__ 는 test_result.txt 값으로 교체)
```

**A-G5. 기능2 베이스라인 vs 개발모델**
```
막대그래프. 제목 '기능2 베이스라인 vs 개발 모델 (Accuracy)'.
- 베이스라인 (TF-IDF + 로지스틱회귀): 0.5501
- 개발 모델 (KoELECTRA-small): 0.64
개발 모델을 강조색으로, '+0.09 향상'을 화살표/주석으로 표시. y축 0~1, 한글 라벨.
```

**A-G6. 기능2 클래스별 F1**
```
가로 막대그래프. 제목 '기능2 클래스별 F1-score (KoELECTRA)'.
- 비공격(0): 0.80
- 약한공격(1): 0.51
- 강한공격(2): 0.49
0.6 미만 막대(약·강 공격)는 빨간 계열로 강조해 약점을 부각. 막대 끝 수치, 한글 라벨.
```

**A-G7. 기능3 상태 리포트 히트맵 (예시)**
```
히트맵 형태의 표를 그려줘. 제목 '기능3 날짜별·발신자별 상태 리포트 (예시)'.
- 행: 발신자 User_A ~ User_D
- 열: 최근 7일 날짜
- 각 셀: 🟢평소와비슷 / 🟡조금다름 / 🟠꽤다름 / ⚪데이터부족 4색 신호등으로 채운 예시
범례 포함, 한글 라벨. (실제 색은 daily_report.json 결과로 교체 권장)
```
